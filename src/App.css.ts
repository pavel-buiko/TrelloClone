import { style } from "@vanilla-extract/css";

export const selectBoard = style({
  width: "65%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});

export const selectBoardTitle = style({
  padding: "2rem 4rem",
  borderRadius: "20px",
  background: "#f2f2f2",
});
